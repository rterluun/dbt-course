dbtcourse:
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: database
      schema: 'main'
      schemas_and_paths:
        main: "{{ env_var('PWD') }}/dbs/dbtcourse.db"
      schema_directory: "{{ env_var('PWD') }}/dbs"
  target: dev
